---
created_by: John Hoff
created_on: '2026-07-25T15:15:39Z'
name: v0.1.1-mcp-transition
status: open
updated_by: John Hoff
updated_on: '2026-07-25T19:47:01Z'
---

Transition project interaction with .sourcebook from CLI shell-outs (`cac world`/`lore`/`region`/`campaign`/`encounter`) to an MCP server, per the `mcp/` placeholder already noted in CLAUDE.md and the framework's stated end-state: the coding assistant should have no awareness of the `.sourcebook` directory's existence at all. Covers building an MCP server that exposes the same domain operations as the CLI, and migrating the `world-manager`/`campaign-manager` skills to call that server instead of shelling out to `cac`.
