---
archived: true
created_by: John Hoff
created_on: '2026-07-25T15:16:02Z'
name: v0.1.3-skill-and-configuration-bundling
status: completed
updated_by: John Hoff
updated_on: '2026-08-04T06:47:13Z'
---

Pull the `world-manager` and `campaign-manager` agent skills - currently authored directly under `.claude/skills/` and `.agents/skills/` in this repository - into `packages/crypts-and-commits/src/cac/core/templates/skills/`, and have `cac bootstrap` deploy them (along with the `.sourcebook/**` Edit/Write deny rule in `.claude/settings.json`) into a target project's own `.claude/settings.json`/skills directories, the same way `world.md` is templated into `.sourcebook/world.md` today. This migration is explicitly called out in CLAUDE.md as forward-looking, gated on the skills being fully bootstrapped and working first - which v0.1.0-bootstrapping has now established.

## Log

### Completed - 2026-07-28T02:57:13Z - John Hoff

Both encounters delivered the scoped work: template-and-deploy-agent-skills packages the world-manager/campaign-manager skills (Claude Code and Codex flavors) under core/templates/skills/ and deploys them via cac bootstrap init's new deploy_skills(), overwriting on every run so framework updates propagate. deploy-codex-sourcebook-guardrail (driven separately by the developer) extends bootstrap to deploy a packaged Codex PreToolUse hook blocking direct shell/apply_patch edits to .sourcebook, closing the guardrail gap Codex itself raised - deliberately not mirrored to Claude Code, whose declarative Edit(.sourcebook/**) deny rule is preferred there for clarity of intent even though it doesn't block Bash-based bypass; that tradeoff is accepted, not a gap to close. CLAUDE.md's "Agent skills" section, which had described this migration as deferred/forward-looking, was updated to describe the now-shipped deployment mechanism and to flag that this repo's own .claude/skills//.agents/skills/ stay hand-edited source-of-truth copies requiring manual re-sync into templates/skills/ when changed. Full pytest suite and ruff check/format clean throughout.
